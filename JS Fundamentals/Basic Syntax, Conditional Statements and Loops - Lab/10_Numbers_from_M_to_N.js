// Write a function that receives a number M and a number N (M will always be bigger than N). 
// Print all numbers from M to N.

function numbersBetween(first, second) {
    for (let i = first; i >= second; i--)
    console.log(i)
}